{
  "name": "{{ name }}",
  "version": "0.0.0",
  "description": "{{ description }}",
  "main": "electron.js",
  "dependencies": {
    {{ver vueVersion 'core'}}{{deps plugins vueVersion}}
  },
  "devDependencies": {},
  "author": "{{ author }}"
}
