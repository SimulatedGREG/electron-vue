{
  "name": "{{ name }}",
  "version": "0.0.0",
  "description": "{{ description }}",
  "main": "electron.js",
  "dependencies": {
    "vue": "^2.0.1"{{deps plugins}}
  },
  "devDependencies": {},
  "author": "{{ author }}"
}
